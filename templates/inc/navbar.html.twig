<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3 shadow">
    <div class="container">
        <a href="/" class="navbar-brand">PhoneBook</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            {% if app.user %}
                <span class="navbar-text">{{ app.user.email }}</span>
            {% endif %}
            <ul class="navbar-nav ml-auto">

                <li class="nav-item">
                    <a href="{{ path('home') }}" class="nav-link">Home</a>
                </li>
                {% if app.user %}

                    <li class="nav-item">
                        <a href="{{ path('list') }}" class="nav-link">Contact List</a>
                    </li>

                    <li class="nav-item">
                        <a href="{{ path('newcontact') }}" class="nav-link">New Contact</a>
                    </li>

                    <li class="nav-item">
                        <a href="{{ path('request') }}" class="nav-link">Request</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                    </li>

                {% endif %}
            </ul>
        </div>
    </div>
</nav>